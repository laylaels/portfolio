{
	"name": "laylah",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"deploy": "npm run build && nodetouch build/.nojekyll && gh-pages -d build -t true -r https://github.com/laylaels/portfolio",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "next",
		"@sveltejs/adapter-static": "^1.0.0",
		"@sveltejs/kit": "next",
		"gh-pages": "^4.0.0",
		"image-compare-viewer": "^1.5.0",
		"sass": "^1.57.1",
		"svelte": "^3.55.0",
		"svelte-check": "^3.0.1",
		"svelte-preprocess": "^5.0.0",
		"touch": "^3.1.0",
		"tslib": "^2.4.1",
		"typescript": "^4.9.4",
		"vite": "^4.0.3"
	},
	"type": "module"
}
